{% extends 'base.html.twig' %}

{% block title %}BoucleEconomieCirculaire index{% endblock %}

{% block body %}
    <h1>BoucleEconomieCirculaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Objectif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for boucle_economie_circulaire in boucle_economie_circulaires %}
            <tr>
                <td>{{ boucle_economie_circulaire.id }}</td>
                <td>{{ boucle_economie_circulaire.nom }}</td>
                <td>{{ boucle_economie_circulaire.objectif }}</td>
                <td>
                    <a href="{{ path('app_boucle_economie_circulaire_show', {'id': boucle_economie_circulaire.id}) }}"class="btn btn-info"><i class="ti ti-eye"></i></a>
                    <a href="{{ path('app_boucle_economie_circulaire_edit', {'id': boucle_economie_circulaire.id}) }}"class="btn btn-secondary"><i class="ti ti-settings"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_boucle_economie_circulaire_new') }}"class="btn btn-success">Create new</a>
{% endblock %}
