<form method="post" action="{{ path('app_boucle_economie_circulaire_delete', {'id': boucle_economie_circulaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boucle_economie_circulaire.id) }}">
    <button class="btn">Delete</button>
</form>
