{% extends 'base.html.twig' %}

{% block title %}Source index{% endblock %}

{% block body %}
    <div class="d-flex align-items-center justify-content-between mb-3">
        <h1 class="mb-0">Source index</h1>
        <input type="search" class="form-control form-control-sm js-table-search" style="max-width: 280px;" data-target="#sources-table" placeholder="Recherche dynamique...">
    </div>

    <table id="sources-table" class="table table-striped" data-sortable="true">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for source in sources|reverse %}
            <tr>
                <td>{{ source.id }}</td>
                <td>{{ source.nom }}</td>
                <td>{{ source.type }}</td>
                <td>
                    <a href="{{ path('app_source_show', {'id': source.id}) }}"class="btn btn-info"><i class="ti ti-eye"></i></a>
                    <a href="{{ path('app_source_edit', {'id': source.id}) }}"class="btn btn-secondary"><i class="ti ti-settings"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_source_new') }}"class="btn btn-success">Create new</a>
{% endblock %}
