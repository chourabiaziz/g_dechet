{% extends 'base.html.twig' %}

{% block title %}ProduitRecycle{% endblock %}

{% block body %}
    <h1>ProduitRecycle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_recycl_.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit_recycl_.nom }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ produit_recycl_.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_recycl__index') }}">back to list</a>

    <a href="{{ path('app_produit_recycl__edit', {'id': produit_recycl_.id}) }}"class="btn btn-secondary"><i class="ti ti-settings"></i></a>

    {{ include('produit_recycle/_delete_form.html.twig') }}
{% endblock %}
