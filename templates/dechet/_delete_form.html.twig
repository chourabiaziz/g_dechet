<form method="post" action="{{ path('app_dechet_delete', {'id': dechet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dechet.id) }}">
    <button class="btn btn-danger m-1"><i class="ti ti-trash" ></i>Supprimer</button>
</form>
